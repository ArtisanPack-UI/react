/**
 * @module common
 *
 * Re-exports shared, framework-agnostic types from `@artisanpack-ui/tokens`.
 * These types are used across all ArtisanPack UI React components.
 *
 * @packageDocumentation
 */

/**
 * {@inheritDoc @artisanpack-ui/tokens#DaisyColor}
 *
 * DaisyUI semantic color names: `'primary'`, `'secondary'`, `'accent'`,
 * `'success'`, `'warning'`, `'error'`, `'info'`, `'neutral'`.
 */
/**
 * {@inheritDoc @artisanpack-ui/tokens#Size}
 *
 * Component size scale: `'xs'`, `'sm'`, `'md'`, `'lg'`.
 */
/**
 * {@inheritDoc @artisanpack-ui/tokens#GlassProps}
 *
 * Props for components that support glass morphism effects.
 */
/**
 * {@inheritDoc @artisanpack-ui/tokens#ColorProps}
 *
 * Props for components that accept a DaisyUI color variant.
 */
export type { DaisyColor, Size, GlassProps, ColorProps } from '@artisanpack-ui/tokens';
