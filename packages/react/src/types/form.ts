/**
 * @module form
 *
 * Re-exports form-related types from `@artisanpack-ui/tokens`.
 *
 * @packageDocumentation
 */

/**
 * {@inheritDoc @artisanpack-ui/tokens#FormFieldProps}
 *
 * Common props shared by form field components such as Input, Select,
 * and Textarea. Includes `label`, `error`, and `hint` strings.
 */
export type { FormFieldProps } from '@artisanpack-ui/tokens';
